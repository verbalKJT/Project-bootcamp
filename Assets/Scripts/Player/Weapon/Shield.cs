using Photon.Pun;
using UnityEngine;

public class Shield : MonoBehaviourPun
{
    [SerializeField] private GameObject shield; // 방패 하위 Plane

    void Start()
    {
    }

    // Update is called once per frame
    void Update()
    {
    }
}